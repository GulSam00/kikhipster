Generic single-database configuration (async 미사용).

로컬 실행 예:
    cd backend
    alembic upgrade head

배포 시에는 Dockerfile entrypoint가 아니라 별도 배포 단계(예: aws ecs run-task)에서
`alembic upgrade head`를 1회만 실행한다. 컨테이너 기동 로직에 넣으면 태스크가
여러 개로 늘어날 때 동시 실행될 위험이 있다.
